// next
import type { Metadata } from "next";

// components
import B from "@/components/course/content/B";

export const metadata: Metadata = {
  title: "ArtCom ► AutoCAD GEOLIPS - tworzenie map numerycznych",
};

export default function Page() {
  return <B />;
}
